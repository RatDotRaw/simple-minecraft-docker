FROM eclipse-temurin:17-jre

ADD ./data /app/data

RUN apt update && apt install --no-install-recommends -y wget unzip nodejs npm && rm -rf /var/lib/apt/lists/*


# FROM mcspre

# EXPOSE 25565

# ADD ./rcon /app/rcon
# WORKDIR /app/rcon
# RUN npm install
# RUN ln -s /app/rcon/rcon.js /usr/bin/rcon
# RUN chmod u+x /app/rcon/rcon.js

# WORKDIR /app/container

# CMD ["bash", "/app/data/startup.sh"]


# docker build -t mcspre -f DockerfilePre .